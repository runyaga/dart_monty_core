// FFI binding for the output-depth truncation property (PT-S2.1).
@Tags(['integration', 'ffi'])
library;

import 'package:test/test.dart';

import '_output_depth_test_body.dart';

void main() => runOutputDepthTests();
