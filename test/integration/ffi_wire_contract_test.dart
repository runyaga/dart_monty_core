// FFI binding for the wire-contract type-identity property (Phase 1 of
// BRIDGE-EXECUTE.md).
@Tags(['integration', 'ffi'])
library;

import 'package:test/test.dart';

import '_wire_contract_test_body.dart';

void main() => runWireContractTests();
