// FFI binding for the repr differential (core#130).
@Tags(['integration', 'ffi'])
library;

import 'package:test/test.dart';

import '_repr_oracle_test_body.dart';

void main() => runReprOracleTests();
